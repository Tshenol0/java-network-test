Java Network Test

A minimal Java example that sends a message from one computer to another using the target computer’s IP address, implemented entirely with standard (vanilla) Java and no external libraries.
